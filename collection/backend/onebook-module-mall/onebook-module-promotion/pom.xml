<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.techtron</groupId>
        <artifactId>onebook-module-mall</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    <artifactId>onebook-module-promotion</artifactId>

    <name>${project.artifactId}</name>

    <description>
        promotion 模块，主要实现营销相关功能
        例如：营销活动、banner 广告、优惠券、优惠码等功能。
    </description>

    <dependencies>
        <dependency>
            <groupId>com.techtron</groupId>
            <artifactId>onebook-module-product</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.techtron</groupId>
            <artifactId>onebook-module-trade-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.techtron</groupId>
            <artifactId>onebook-module-system</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.techtron</groupId>
            <artifactId>onebook-module-infra</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.techtron</groupId>
            <artifactId>onebook-module-member</artifactId>
            <version>${revision}</version>
        </dependency>


        <!-- Web 相关 -->
        <dependency>
            <groupId>com.techtron</groupId>
            <artifactId>onebook-spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.techtron</groupId>
            <artifactId>onebook-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>com.techtron</groupId>
            <artifactId>onebook-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>com.techtron</groupId>
            <artifactId>onebook-spring-boot-starter-excel</artifactId>
        </dependency>
    </dependencies>

</project>
