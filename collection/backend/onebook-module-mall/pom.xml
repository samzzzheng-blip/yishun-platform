<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>onebook</artifactId>
        <groupId>com.techtron</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>onebook-module-mall</artifactId>
    <packaging>pom</packaging>

    <name>${project.artifactId}</name>

    <description>
        商城大模块，由 product 商品、promotion 营销、trade 交易、statistics 统计等组成
    </description>
    <modules>
        <module>onebook-module-product</module>
        <module>onebook-module-promotion</module>
        <module>onebook-module-trade</module>
        <module>onebook-module-statistics</module>
        <!--
            特殊：为什么会有 onebook-module-trade-api 呢？
                onebook-module-promotion 和 onebook-module-trade 之间相互循环依赖，所以抽出 onebook-module-trade-api 模块，这样：
                1. onebook-module-promotion 依赖 onebook-module-trade-api
                2. onebook-module-trade 依赖 onebook-module-promotion
            从而不存在相互（循环）依赖，即 onebook-module-trade => onebook-module-promotion => onebook-module-trade-api
         -->
        <module>onebook-module-trade-api</module>
    </modules>

</project>
