# 验收及交接

## 本次范围

整理两套服务端、两套管理网页、收藏小程序与原生证书小程序；保留本地变更；归档历史补丁源码；移除运行数据及秘密；写明构建、部署、回退和数据恢复边界。

## 验证记录

- 整理前收藏后台图片排序构建成功，排序相关后端测试 3 项通过。
- 2026-09-23 图片排序线上服务启动正常，首页及新 JS/CSS 与构建逐字节一致；尚未完成登录后的实际拖动保存验证。
- 本次 GitHub 整理不等于对所有历史功能重新完整验收。
- 整理后检查结果见 `validation.json`，文件清单见 `source-manifest.json`。

本次整理后的两套服务端 Maven package 与两个管理前端生产构建均通过。未在生产运行归档副本；依赖复用了本机安装，未验证全新依赖安装。证书网页使用 Node 18.20.8，并仅在临时构建配置禁用 symlink 解析以适配外置依赖目录。

## 接手后优先事项

- 补全本地配置并分别构建两个服务端和前端。
- 在测试环境确认签名证书原始源码与当前线上补丁的功能差异，再决定整体源码部署方案。
- 确认小程序本地未发布修改，避免打包时混入未知改动。
- 检查用户305关联订单历史反馈是否已在后续版本修复；本归档不能据此前排查宣称修复完成。
- 完成数据库和媒体恢复演练。
- 将凭据统一到受保护配置中，避免未来重新硬编码。

## 历史脚本

`operations` 用于溯源和人工审查。生成产物、数据库内容和部分本地基线未随仓库上传，因此不是开箱即用的批量部署工具；执行前必须确认输入、目标及回退。
